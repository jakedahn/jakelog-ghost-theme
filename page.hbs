{{!< default}}
{{#post}}

<main class="content row">
    <article class="{{post_class}} col-md-9">
        <header class="post-header">
            <h1 class="post-title">{{title}}</h1>
        </header>

        <section class="post-content">
            {{content}}
        </section>
    </article>
</main>

{{/post}}
